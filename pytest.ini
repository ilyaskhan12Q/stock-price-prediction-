[pytest]
filterwarnings =
    # NumPy 2.0 copy-keyword change — comes from inside Keras, not our code
    ignore::DeprecationWarning:keras.*
    ignore::DeprecationWarning:tensorflow.*
    # General deprecation noise from third-party libs
    ignore::PendingDeprecationWarning
